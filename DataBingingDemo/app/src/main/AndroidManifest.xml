<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.axon.databingingdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EventHanderActivity" />
        <activity android:name=".BaseUserActivity" />
        <activity android:name=".DoubleBindingActivity" />
        <activity android:name=".ObservableActivity" />
        <activity android:name=".LayoutDetailActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".MultiRecyclerViewActivity" />
        <activity android:name=".CustomAttributeActivity"/>
        <activity android:name=".ViewStubActivity" />
    </application>

</manifest>